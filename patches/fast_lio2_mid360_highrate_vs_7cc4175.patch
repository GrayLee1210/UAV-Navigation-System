diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5f91a33..fe2cdd0 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -51,7 +51,7 @@ find_package(catkin REQUIRED COMPONENTS
   std_msgs
   pcl_ros
   tf
-  livox_ros_driver
+  livox_ros_driver2
   message_generation
   eigen_conversions
 )
@@ -86,4 +86,4 @@ catkin_package(
 
 add_executable(fastlio_mapping src/laserMapping.cpp include/ikd-Tree/ikd_Tree.cpp src/preprocess.cpp)
 target_link_libraries(fastlio_mapping ${catkin_LIBRARIES} ${PCL_LIBRARIES} ${PYTHON_LIBRARIES})
-target_include_directories(fastlio_mapping PRIVATE ${PYTHON_INCLUDE_DIRS})
\ No newline at end of file
+target_include_directories(fastlio_mapping PRIVATE ${PYTHON_INCLUDE_DIRS})
diff --git a/config/mid360.yaml b/config/mid360.yaml
index 20c0e01..61f6434 100644
--- a/config/mid360.yaml
+++ b/config/mid360.yaml
@@ -28,8 +28,11 @@ publish:
     scan_publish_en:  true       # false: close all the point cloud output
     dense_publish_en: true       # false: low down the points number in a global-frame point clouds scan.
     scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame
+    highrate_odom_en: true
+    highrate_odom_topic: "/Odometry_highrate"
+    highrate_odom_max_dt: 0.05
 
 pcd_save:
-    pcd_save_en: true
+    pcd_save_en: false
     interval: -1                 # how many LiDAR frames saved in each pcd file; 
                                  # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
diff --git a/include/IKFoM_toolkit/esekfom/esekfom.hpp b/include/IKFoM_toolkit/esekfom/esekfom.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/esekfom/util.hpp b/include/IKFoM_toolkit/esekfom/util.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/build_manifold.hpp b/include/IKFoM_toolkit/mtk/build_manifold.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/src/SubManifold.hpp b/include/IKFoM_toolkit/mtk/src/SubManifold.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/src/mtkmath.hpp b/include/IKFoM_toolkit/mtk/src/mtkmath.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/src/vectview.hpp b/include/IKFoM_toolkit/mtk/src/vectview.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/startIdx.hpp b/include/IKFoM_toolkit/mtk/startIdx.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/types/S2.hpp b/include/IKFoM_toolkit/mtk/types/S2.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/types/SOn.hpp b/include/IKFoM_toolkit/mtk/types/SOn.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/types/vect.hpp b/include/IKFoM_toolkit/mtk/types/vect.hpp
old mode 100755
new mode 100644
diff --git a/include/IKFoM_toolkit/mtk/types/wrapped_cv_mat.hpp b/include/IKFoM_toolkit/mtk/types/wrapped_cv_mat.hpp
old mode 100755
new mode 100644
diff --git a/package.xml b/package.xml
index 98bc774..1e8c4d1 100644
--- a/package.xml
+++ b/package.xml
@@ -25,7 +25,7 @@
   <build_depend>sensor_msgs</build_depend>
   <build_depend>tf</build_depend>
   <build_depend>pcl_ros</build_depend>
-  <build_depend>livox_ros_driver</build_depend>
+  <build_depend>livox_ros_driver2</build_depend>
   <build_depend>message_generation</build_depend>
 
   <run_depend>geometry_msgs</run_depend>
@@ -36,7 +36,7 @@
   <run_depend>std_msgs</run_depend>
   <run_depend>tf</run_depend>
   <run_depend>pcl_ros</run_depend>
-  <run_depend>livox_ros_driver</run_depend>
+  <run_depend>livox_ros_driver2</run_depend>
   <run_depend>message_runtime</run_depend>
 
   <test_depend>rostest</test_depend>
diff --git a/src/IMU_Processing.hpp b/src/IMU_Processing.hpp
index 344d07f..d7b4399 100644
--- a/src/IMU_Processing.hpp
+++ b/src/IMU_Processing.hpp
@@ -51,6 +51,12 @@ class ImuProcess
   void set_acc_bias_cov(const V3D &b_a);
   Eigen::Matrix<double, 12, 12> Q;
   void Process(const MeasureGroup &meas,  esekfom::esekf<state_ikfom, 12, input_ikfom> &kf_state, PointCloudXYZI::Ptr pcl_un_);
+  bool PropagateHighRateState(const sensor_msgs::ImuConstPtr &last_imu,
+                              const sensor_msgs::ImuConstPtr &cur_imu,
+                              state_ikfom &state_io,
+                              V3D *acc_world = nullptr,
+                              V3D *gyr_unbiased = nullptr,
+                              double max_dt = 0.05);
 
   ofstream fout_imu;
   V3D cov_acc;
@@ -213,6 +219,49 @@ void ImuProcess::IMU_init(const MeasureGroup &meas, esekfom::esekf<state_ikfom,
 
 }
 
+bool ImuProcess::PropagateHighRateState(const sensor_msgs::ImuConstPtr &last_imu,
+                                        const sensor_msgs::ImuConstPtr &cur_imu,
+                                        state_ikfom &state_io,
+                                        V3D *acc_world,
+                                        V3D *gyr_unbiased,
+                                        double max_dt)
+{
+  if (imu_need_init_ || !last_imu || !cur_imu) return false;
+
+  const double dt = cur_imu->header.stamp.toSec() - last_imu->header.stamp.toSec();
+  if (dt <= 0.0 || dt > max_dt) return false;
+
+  const double mean_acc_norm = mean_acc.norm();
+  if (mean_acc_norm < 1e-6) return false;
+
+  V3D acc_avr, gyr_avr;
+  gyr_avr << 0.5 * (last_imu->angular_velocity.x + cur_imu->angular_velocity.x),
+             0.5 * (last_imu->angular_velocity.y + cur_imu->angular_velocity.y),
+             0.5 * (last_imu->angular_velocity.z + cur_imu->angular_velocity.z);
+  acc_avr << 0.5 * (last_imu->linear_acceleration.x + cur_imu->linear_acceleration.x),
+             0.5 * (last_imu->linear_acceleration.y + cur_imu->linear_acceleration.y),
+             0.5 * (last_imu->linear_acceleration.z + cur_imu->linear_acceleration.z);
+
+  acc_avr = acc_avr * G_m_s2 / mean_acc_norm;
+  const V3D gyr_body = gyr_avr - state_io.bg;
+  const V3D acc_inertial = state_io.rot * (acc_avr - state_io.ba);
+
+  V3D acc_total = acc_inertial;
+  for (int i = 0; i < 3; ++i)
+  {
+    acc_total[i] += state_io.grav[i];
+  }
+
+  state_io.pos += state_io.vel * dt + 0.5 * acc_total * dt * dt;
+  state_io.vel += acc_total * dt;
+  state_io.rot = SO3(state_io.rot.toRotationMatrix() * Exp(gyr_body, dt));
+
+  if (acc_world) *acc_world = acc_total;
+  if (gyr_unbiased) *gyr_unbiased = gyr_body;
+
+  return true;
+}
+
 void ImuProcess::UndistortPcl(const MeasureGroup &meas, esekfom::esekf<state_ikfom, 12, input_ikfom> &kf_state, PointCloudXYZI &pcl_out)
 {
   /*** add the imu of the last frame-tail to the of current frame-head ***/
diff --git a/src/laserMapping.cpp b/src/laserMapping.cpp
index fc41408..4af214d 100644
--- a/src/laserMapping.cpp
+++ b/src/laserMapping.cpp
@@ -56,7 +56,7 @@
 #include <tf/transform_datatypes.h>
 #include <tf/transform_broadcaster.h>
 #include <geometry_msgs/Vector3.h>
-#include <livox_ros_driver/CustomMsg.h>
+#include <livox_ros_driver2/CustomMsg.h>
 #include "preprocess.h"
 #include <ikd-Tree/ikd_Tree.h>
 
@@ -94,7 +94,10 @@ int    iterCount = 0, feats_down_size = 0, NUM_MAX_ITERATIONS = 0, laserCloudVal
 bool   point_selected_surf[100000] = {0};
 bool   lidar_pushed, flg_first_scan = true, flg_exit = false, flg_EKF_inited;
 bool   scan_pub_en = false, dense_pub_en = false, scan_body_pub_en = false;
+bool   highrate_odom_en = true, highrate_odom_ready = false;
 int lidar_type;
+double highrate_odom_max_dt = 0.05;
+string highrate_odom_topic = "/Odometry_highrate";
 
 vector<vector<int>>  pointSearchInd_surf; 
 vector<BoxPointType> cub_needrm;
@@ -130,12 +133,17 @@ M3D Lidar_R_wrt_IMU(Eye3d);
 MeasureGroup Measures;
 esekfom::esekf<state_ikfom, 12, input_ikfom> kf;
 state_ikfom state_point;
+state_ikfom state_highrate;
 vect3 pos_lid;
 
 nav_msgs::Path path;
 nav_msgs::Odometry odomAftMapped;
+nav_msgs::Odometry odomHighRate;
 geometry_msgs::Quaternion geoQuat;
 geometry_msgs::PoseStamped msg_body_pose;
+ros::Publisher pubOdomHighRate;
+mutex mtx_highrate_odom;
+sensor_msgs::Imu::ConstPtr last_highrate_imu;
 
 shared_ptr<Preprocess> p_pre(new Preprocess());
 shared_ptr<ImuProcess> p_imu(new ImuProcess());
@@ -276,6 +284,41 @@ void lasermap_fov_segment()
     kdtree_delete_time = omp_get_wtime() - delete_begin;
 }
 
+void fill_odom_msg_from_state(nav_msgs::Odometry &odom,
+                              const state_ikfom &state,
+                              const ros::Time &stamp,
+                              const V3D &angular_vel)
+{
+    odom.header.frame_id = "camera_init";
+    odom.child_frame_id = "body";
+    odom.header.stamp = stamp;
+
+    odom.pose.pose.position.x = state.pos(0);
+    odom.pose.pose.position.y = state.pos(1);
+    odom.pose.pose.position.z = state.pos(2);
+    odom.pose.pose.orientation.x = state.rot.coeffs()[0];
+    odom.pose.pose.orientation.y = state.rot.coeffs()[1];
+    odom.pose.pose.orientation.z = state.rot.coeffs()[2];
+    odom.pose.pose.orientation.w = state.rot.coeffs()[3];
+
+    odom.twist.twist.linear.x = state.vel(0);
+    odom.twist.twist.linear.y = state.vel(1);
+    odom.twist.twist.linear.z = state.vel(2);
+    odom.twist.twist.angular.x = angular_vel(0);
+    odom.twist.twist.angular.y = angular_vel(1);
+    odom.twist.twist.angular.z = angular_vel(2);
+}
+
+void publish_highrate_odometry(const state_ikfom &state,
+                               const V3D &angular_vel,
+                               const ros::Time &stamp)
+{
+    if (!highrate_odom_en) return;
+
+    fill_odom_msg_from_state(odomHighRate, state, stamp, angular_vel);
+    pubOdomHighRate.publish(odomHighRate);
+}
+
 void standard_pcl_cbk(const sensor_msgs::PointCloud2::ConstPtr &msg) 
 {
     mtx_buffer.lock();
@@ -299,7 +342,7 @@ void standard_pcl_cbk(const sensor_msgs::PointCloud2::ConstPtr &msg)
 
 double timediff_lidar_wrt_imu = 0.0;
 bool   timediff_set_flg = false;
-void livox_pcl_cbk(const livox_ros_driver::CustomMsg::ConstPtr &msg) 
+void livox_pcl_cbk(const livox_ros_driver2::CustomMsg::ConstPtr &msg) 
 {
     mtx_buffer.lock();
     double preprocess_start_time = omp_get_wtime();
@@ -354,6 +397,9 @@ void imu_cbk(const sensor_msgs::Imu::ConstPtr &msg_in)
     {
         ROS_WARN("imu loop back, clear buffer");
         imu_buffer.clear();
+        lock_guard<mutex> lock(mtx_highrate_odom);
+        highrate_odom_ready = false;
+        last_highrate_imu.reset();
     }
 
     last_timestamp_imu = timestamp;
@@ -361,6 +407,37 @@ void imu_cbk(const sensor_msgs::Imu::ConstPtr &msg_in)
     imu_buffer.push_back(msg);
     mtx_buffer.unlock();
     sig_buffer.notify_all();
+
+    if (highrate_odom_en)
+    {
+        state_ikfom state_snapshot;
+        V3D acc_world = Zero3d;
+        V3D gyr_unbiased = Zero3d;
+        bool publish_highrate = false;
+
+        {
+            lock_guard<mutex> lock(mtx_highrate_odom);
+            if (highrate_odom_ready && last_highrate_imu)
+            {
+                publish_highrate = p_imu->PropagateHighRateState(last_highrate_imu,
+                                                                 msg,
+                                                                 state_highrate,
+                                                                 &acc_world,
+                                                                 &gyr_unbiased,
+                                                                 highrate_odom_max_dt);
+                if (publish_highrate)
+                {
+                    state_snapshot = state_highrate;
+                }
+            }
+            last_highrate_imu = msg;
+        }
+
+        if (publish_highrate)
+        {
+            publish_highrate_odometry(state_snapshot, gyr_unbiased, msg->header.stamp);
+        }
+    }
 }
 
 double lidar_mean_scantime = 0.0;
@@ -588,11 +665,7 @@ void set_posestamp(T & out)
 
 void publish_odometry(const ros::Publisher & pubOdomAftMapped)
 {
-    odomAftMapped.header.frame_id = "camera_init";
-    odomAftMapped.child_frame_id = "body";
-    odomAftMapped.header.stamp = ros::Time().fromSec(lidar_end_time);// ros::Time().fromSec(lidar_end_time);
-    set_posestamp(odomAftMapped.pose);
-    pubOdomAftMapped.publish(odomAftMapped);
+    fill_odom_msg_from_state(odomAftMapped, state_point, ros::Time().fromSec(lidar_end_time), Zero3d);
     auto P = kf.get_P();
     for (int i = 0; i < 6; i ++)
     {
@@ -604,6 +677,7 @@ void publish_odometry(const ros::Publisher & pubOdomAftMapped)
         odomAftMapped.pose.covariance[i*6 + 4] = P(k, 1);
         odomAftMapped.pose.covariance[i*6 + 5] = P(k, 2);
     }
+    pubOdomAftMapped.publish(odomAftMapped);
 
     static tf::TransformBroadcaster br;
     tf::Transform                   transform;
@@ -762,6 +836,9 @@ int main(int argc, char** argv)
     nh.param<bool>("publish/scan_publish_en",scan_pub_en, true);
     nh.param<bool>("publish/dense_publish_en",dense_pub_en, true);
     nh.param<bool>("publish/scan_bodyframe_pub_en",scan_body_pub_en, true);
+    nh.param<bool>("publish/highrate_odom_en", highrate_odom_en, true);
+    nh.param<string>("publish/highrate_odom_topic", highrate_odom_topic, string("/Odometry_highrate"));
+    nh.param<double>("publish/highrate_odom_max_dt", highrate_odom_max_dt, 0.05);
     nh.param<int>("max_iteration",NUM_MAX_ITERATIONS,4);
     nh.param<string>("map_file_path",map_file_path,"");
     nh.param<string>("common/lid_topic",lid_topic,"/livox/lidar");
@@ -856,6 +933,8 @@ int main(int argc, char** argv)
             ("/Laser_map", 100000);
     ros::Publisher pubOdomAftMapped = nh.advertise<nav_msgs::Odometry> 
             ("/Odometry", 100000);
+    pubOdomHighRate = nh.advertise<nav_msgs::Odometry>
+            (highrate_odom_topic, 100000);
     ros::Publisher pubPath          = nh.advertise<nav_msgs::Path> 
             ("/path", 100000);
 //------------------------------------------------------------------------------------------------------
@@ -966,6 +1045,23 @@ int main(int argc, char** argv)
             geoQuat.z = state_point.rot.coeffs()[2];
             geoQuat.w = state_point.rot.coeffs()[3];
 
+            if (highrate_odom_en)
+            {
+                lock_guard<mutex> lock(mtx_highrate_odom);
+                state_highrate = state_point;
+                if (!Measures.imu.empty())
+                {
+                    sensor_msgs::Imu::Ptr imu_anchor(new sensor_msgs::Imu(*Measures.imu.back()));
+                    imu_anchor->header.stamp = ros::Time().fromSec(lidar_end_time);
+                    last_highrate_imu = imu_anchor;
+                }
+                else
+                {
+                    last_highrate_imu.reset();
+                }
+                highrate_odom_ready = true;
+            }
+
             double t_update_end = omp_get_wtime();
 
             /******* Publish odometry *******/
diff --git a/src/preprocess.cpp b/src/preprocess.cpp
index 09a3959..552ea64 100644
--- a/src/preprocess.cpp
+++ b/src/preprocess.cpp
@@ -41,7 +41,7 @@ void Preprocess::set(bool feat_en, int lid_type, double bld, int pfilt_num)
   point_filter_num = pfilt_num;
 }
 
-void Preprocess::process(const livox_ros_driver::CustomMsg::ConstPtr &msg, PointCloudXYZI::Ptr &pcl_out)
+void Preprocess::process(const livox_ros_driver2::CustomMsg::ConstPtr &msg, PointCloudXYZI::Ptr &pcl_out)
 {  
   avia_handler(msg);
   *pcl_out = pl_surf;
@@ -89,7 +89,7 @@ void Preprocess::process(const sensor_msgs::PointCloud2::ConstPtr &msg, PointClo
   *pcl_out = pl_surf;
 }
 
-void Preprocess::avia_handler(const livox_ros_driver::CustomMsg::ConstPtr &msg)
+void Preprocess::avia_handler(const livox_ros_driver2::CustomMsg::ConstPtr &msg)
 {
   pl_surf.clear();
   pl_corn.clear();
diff --git a/src/preprocess.h b/src/preprocess.h
index 35f3833..b18f2b6 100644
--- a/src/preprocess.h
+++ b/src/preprocess.h
@@ -5,7 +5,7 @@
 #include <ros/ros.h>
 #include <pcl_conversions/pcl_conversions.h>
 #include <sensor_msgs/PointCloud2.h>
-#include <livox_ros_driver/CustomMsg.h>
+#include <livox_ros_driver2/CustomMsg.h>
 
 using namespace std;
 
@@ -91,7 +91,7 @@ class Preprocess
   Preprocess();
   ~Preprocess();
   
-  void process(const livox_ros_driver::CustomMsg::ConstPtr &msg, PointCloudXYZI::Ptr &pcl_out);
+  void process(const livox_ros_driver2::CustomMsg::ConstPtr &msg, PointCloudXYZI::Ptr &pcl_out);
   void process(const sensor_msgs::PointCloud2::ConstPtr &msg, PointCloudXYZI::Ptr &pcl_out);
   void set(bool feat_en, int lid_type, double bld, int pfilt_num);
 
@@ -107,7 +107,7 @@ class Preprocess
     
 
   private:
-  void avia_handler(const livox_ros_driver::CustomMsg::ConstPtr &msg);
+  void avia_handler(const livox_ros_driver2::CustomMsg::ConstPtr &msg);
   void oust64_handler(const sensor_msgs::PointCloud2::ConstPtr &msg);
   void velodyne_handler(const sensor_msgs::PointCloud2::ConstPtr &msg);
   void sim_handler(const sensor_msgs::PointCloud2::ConstPtr &msg);
